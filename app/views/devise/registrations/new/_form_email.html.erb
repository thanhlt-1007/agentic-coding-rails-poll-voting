<div>
  <%= f.label :email, class: "block text-sm font-semibold text-gray-700 mb-1" %>
  <div class="relative">
    <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
      <svg class="h-5 w-5 <%= field_icon_color(resource, :email) %>" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"></path>
      </svg>
    </div>
    <%= f.email_field :email, 
        autofocus: true, 
        autocomplete: "email",
        class: "block w-full pl-10 pr-3 py-2.5 border #{field_border_classes(resource, :email)} rounded-lg text-gray-900 placeholder-gray-400 focus:outline-none transition duration-150 ease-in-out sm:text-sm",
        placeholder: t('.email_placeholder') %>
  </div>
  <%= field_error_message(resource, :email) %>
</div>
